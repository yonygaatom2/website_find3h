import { SignInModel } from "../../models/SignInModel"
import { SessionModel } from "../../models/SessionModel"

export const signInUser = async (data: SignInModel): Promise<SessionModel | null> => {
    return null
}